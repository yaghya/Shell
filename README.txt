run the command "make" which will create executable "sh"
run "./sh" to run the created shell
"make clean" to remove .o files
features:
four builtin commands(cd ,echo ,pwd ,exit)
in cd ~ represents the 201502203_Assignment2 directory
cd can do ".." and simple cd
pwd implemented using getcwd
background process is exited with a exit msg

